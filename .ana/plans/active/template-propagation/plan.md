# Plan: template-propagation

**Branch:** feature/template-propagation

## Phases

- [ ] Lock-stock refresh of machine-owned templates on re-init — overwrite agent `.md` instruction bodies and CLAUDE.md from each harness's own stock, preserve basic-config (Claude frontmatter `model`/`tools`; Codex `.agent.toml` `model`/`sandbox_mode`/`model_reasoning_effort`), atomic per-file writes, content-gated consolidated warning, sharpened version nudge, docs/changelog reversal, full preserve-contract regression guard.
  - Spec: spec.md
