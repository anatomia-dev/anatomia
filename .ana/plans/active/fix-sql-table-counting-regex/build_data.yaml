schema: 1
concerns:
  - summary: "Workspace lint reports pre-existing warnings outside this fix: website Hero.tsx and CLI git-operations.ts."
    severity: observation
    suggested_action: monitor
  - summary: "Website build logs cached Next.js warnings for workspace-root inference and proof_chain.json data-cache size while still succeeding."
    severity: observation
    suggested_action: monitor
