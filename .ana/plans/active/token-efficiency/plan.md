# Plan: token-efficiency

**Branch:** feature/token-efficiency

## Phases

- [ ] Phase 1 — `ana test --capture` + the seal-time capture guarantee + sealed token measurement (keystone): capturing runner, three save-time validators, marker/inliner, token ledger with js-tiktoken, `token_economy` proof fold, `ana gain`, `test_json` ana.json override, cross-stack corpus
  - Spec: spec-1.md
- [ ] Phase 2 — enrich the build briefing: inject touched-file symbol sites + ana.json command scalars into `writeWorktreeContext`, soft-fallback, freshness-stamped
  - Spec: spec-2.md
  - Depends on: Phase 1
