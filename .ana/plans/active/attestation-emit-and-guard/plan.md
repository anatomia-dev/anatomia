# Plan: attestation-emit-and-guard

**Branch:** feature/attestation-emit-and-guard

## Phases

- Guard the anatrace-core load (crash → loud abstain) and produce the first real attestation records by installing core + rebuilding the running dist
  - Spec: spec.md
