# Plan: remove-processcapturestrict

**Branch:** feature/remove-processcapturestrict

## Phases

- [ ] Delete the `processCaptureStrict` flag and its `work complete` blocking guard; make the always-record path the only path; rebalance tests
  - Spec: spec.md
