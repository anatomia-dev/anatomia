# Plan: requirements-contract

**Branch:** feature/requirements-contract

## Phases

- The requirements contract — a validated upstream artifact class (`ana req` command group, `validateReqFormat`, status ℹ probe, `--req` claim / archive lifecycle, re-init preservation, Think template touchpoints)
  - Spec: spec.md
