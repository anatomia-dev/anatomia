# Plan: enforcement-state-in-doctor

**Branch:** feature/enforcement-state-in-doctor

## Phases

- [ ] Move enforcement-gate state out of `ana work status` and into a new `Enforcement` dimension in `ana doctor`; fix the `KNOWN_FIELDS` gap for the three gate keys.
  - Spec: spec.md
