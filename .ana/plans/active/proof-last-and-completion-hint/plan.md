# Plan: proof-last-and-completion-hint

**Branch:** feature/proof-last-and-completion-hint

## Phases

- Surface the proof after `work complete` (hint + `next_command`) and add `ana proof --last`/`--latest` with a shared recency comparator
  - Spec: spec.md
