schema: 1
findings:
  - category: code
    summary: "Double path split — detectSurfaces splits relativePath at line 268, then isNonProductPath splits it again at line 85"
    file: "packages/cli/src/engine/detectors/surfaces.ts"
    line: 272
    severity: observation
    suggested_action: accept
    related_assertions: []
  - category: code
    summary: "INFRA_PATTERNS is case-sensitive while EXCLUDED_SEGMENTS is case-insensitive — inconsistent casing strategy between the two pre-filters"
    file: "packages/cli/src/engine/detectors/surfaces.ts"
    line: 45
    severity: debt
    suggested_action: monitor
    related_assertions: ["A011"]
  - category: test
    summary: "Contract file_changes lists state.test.ts but tests were written in monorepoCommandScoping.test.ts — file mismatch between contract and implementation"
    file: "packages/cli/tests/commands/init/monorepoCommandScoping.test.ts"
    severity: observation
    suggested_action: accept
    related_assertions: ["A016", "A017", "A018", "A019", "A020", "A021", "A022"]
  - category: upstream
    summary: "mergeSurfaces console.warn on removed surfaces may be noisy for intentional removals — pre-existing concern from proof context still applies to legitimate orphaned surfaces"
    file: "packages/cli/src/commands/init/state.ts"
    line: 647
    severity: observation
    suggested_action: monitor
  - category: code
    summary: "isNonProductPath returns true for empty string segments from trailing slashes — 'examples/'.split('/') produces ['examples', ''], and '' does not match EXCLUDED_SEGMENTS, so it still works, but edge is unguarded"
    file: "packages/cli/src/engine/detectors/surfaces.ts"
    line: 85
    severity: observation
    suggested_action: accept
