name = "ana-plan"
description = "AnaPlan — reads scope, produces implementation spec."
developer_instructions = "Full instructions in ana-plan.md. Invoke via: ana run"
model = "gpt-5.5"
sandbox_mode = "danger-full-access"
model_reasoning_effort = "high"
