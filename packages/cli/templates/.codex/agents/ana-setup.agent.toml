name = "ana-setup"
description = "Setup orchestrator — calibrates project knowledge."
developer_instructions = "Full instructions in ana-setup.md. Invoke via: ana run"
model = "gpt-5.5"
sandbox_mode = "danger-full-access"
model_reasoning_effort = "high"
